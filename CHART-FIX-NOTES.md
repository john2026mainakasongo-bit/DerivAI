# DerivAI Chart Fix

The Strategy Engine chart was adjusted so the live price plot does not render as a solid black filled area.

Before pushing:
1. npm install
2. npm run build
3. git status
4. git add .
5. git commit -m "Fix Strategy Engine chart rendering"
6. git push origin main
